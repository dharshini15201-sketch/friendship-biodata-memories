package com.friendship.model;\n\nimport lombok.AllArgsConstructor;\nimport lombok.Data;\nimport lombok.NoArgsConstructor;\n\nimport javax.persistence.*;\nimport java.time.LocalDate;\nimport java.time.LocalDateTime;\n\n/**\n * User Entity - Represents a user account in the system\n * \n * Think of this as a blueprint for storing user information in the database\n * Each field corresponds to a column in the 'users' table\n * \n * Key Fields:\n * - userId: Unique ID for each user (like an employee ID)\n * - email: User's email (must be unique)\n * - username: User's login name\n * - passwordHash: Encrypted password (NEVER store plain passwords!)\n * - firstName, lastName: User's full name\n * - bio: About me section\n * - hobbies: What they like to do\n * - dreamGoal: Their life goal\n * - darkModeEnabled: Theme preference\n * - accountStatus: Active/Inactive/Suspended\n * - createdAt: When account was created\n * - lastLogin: Last time user logged in\n */\n@Entity\n@Table(name = \"users\")\n@Data  // Lombok: auto-generates getters, setters, toString\n@NoArgsConstructor  // Lombok: auto-generates default constructor\n@AllArgsConstructor // Lombok: auto-generates constructor with all fields\npublic class User {\n\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Integer userId;\n\n    @Column(unique = true, nullable = false, length = 255)\n    private String email;\n\n    @Column(unique = true, nullable = false, length = 100)\n    private String username;\n\n    @Column(nullable = false, length = 255)\n    private String passwordHash;\n\n    @Column(length = 100)\n    private String firstName;\n\n    @Column(length = 100)\n    private String lastName;\n\n    @Column(length = 100)\n    private String nickname;\n\n    @Column(columnDefinition = \"TEXT\")\n    private String bio;\n\n    @Column(length = 500)\n    private String profilePhotoUrl;\n\n    @Column\n    private LocalDate dateOfBirth;\n\n    @Column(length = 20)\n    private String phone;\n\n    @Enumerated(EnumType.STRING)\n    @Column(nullable = false)\n    private UserType userType = UserType.STUDENT;\n\n    @Column(columnDefinition = \"TEXT\")\n    private String hobbies;\n\n    @Column(length = 50)\n    private String favoriteColor;\n\n    @Column(length = 100)\n    private String favoriteFood;\n\n    @Column(columnDefinition = \"TEXT\")\n    private String dreamGoal;\n\n    @Column\n    private Boolean darkModeEnabled = false;\n\n    @Enumerated(EnumType.STRING)\n    @Column(nullable = false)\n    private AccountStatus accountStatus = AccountStatus.ACTIVE;\n\n    @Column(nullable = false, updatable = false)\n    private LocalDateTime createdAt = LocalDateTime.now();\n\n    @Column\n    private LocalDateTime updatedAt = LocalDateTime.now();\n\n    @Column\n    private LocalDateTime lastLogin;\n\n    // User Type Enum\n    public enum UserType {\n        STUDENT,\n        PROFESSIONAL,\n        OTHER\n    }\n\n    // Account Status Enum\n    public enum AccountStatus {\n        ACTIVE,      // Can use the app\n        INACTIVE,    // Account disabled\n        SUSPENDED    // Account banned\n    }\n}\n