package com.friendship.model;\n\nimport lombok.AllArgsConstructor;\nimport lombok.Data;\nimport lombok.NoArgsConstructor;\n\nimport javax.persistence.*;\nimport java.time.LocalDateTime;\n\n/**\n * FriendProfile Entity - Represents a friendship connection between two users\n * \n * Think of this as: \"User A is friends with User B\"\n * This table connects two users and stores information about their friendship\n * \n * Example:\n * User 1 (Rajesh) and User 2 (Priya) are friends\n * So there's a FriendProfile record:\n * - user_id = 1\n * - friend_id = 2\n * - friendCategory = COLLEGE_FRIEND\n * - friendshipStatus = ACCEPTED\n * - friendshipPercentage = 85 (how similar are they?)\n */\n@Entity\n@Table(name = \"friend_profiles\")\n@Data\n@NoArgsConstructor\n@AllArgsConstructor\npublic class FriendProfile {\n\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Integer friendshipId;\n\n    // Foreign key to User table - who is initiating the friendship\n    @ManyToOne\n    @JoinColumn(name = \"user_id\", nullable = false)\n    private User user;\n\n    // Foreign key to User table - who is the friend\n    @ManyToOne\n    @JoinColumn(name = \"friend_id\", nullable = false)\n    private User friend;\n\n    // What type of friends are they?\n    @Enumerated(EnumType.STRING)\n    @Column(nullable = false)\n    private FriendCategory friendCategory = FriendCategory.OTHER;\n\n    // Status of friendship: PENDING (request sent), ACCEPTED (friends), BLOCKED\n    @Enumerated(EnumType.STRING)\n    @Column(nullable = false)\n    private FriendshipStatus friendshipStatus = FriendshipStatus.PENDING;\n\n    // How compatible are they? (0-100%)\n    // This increases as they fill biodata for each other\n    @Column\n    private Integer friendshipPercentage = 0;\n\n    // How many memories do they have together?\n    @Column\n    private Integer totalMemories = 0;\n\n    // When was there last interaction? (helps show active friendships)\n    @Column\n    private LocalDateTime lastInteraction;\n\n    // When did they become friends?\n    @Column(nullable = false, updatable = false)\n    private LocalDateTime connectedAt = LocalDateTime.now();\n\n    @Column\n    private LocalDateTime updatedAt = LocalDateTime.now();\n\n    // Categories of friendship\n    public enum FriendCategory {\n        SCHOOL_FRIEND,      // Friends from school\n        COLLEGE_FRIEND,     // Friends from college\n        OFFICE_FRIEND,      // Colleagues\n        BEST_FRIEND,        // Close friend\n        OTHER               // Other\n    }\n\n    // Status of the friendship\n    public enum FriendshipStatus {\n        PENDING,    // Friend request sent but not accepted\n        ACCEPTED,   // Both are confirmed friends\n        BLOCKED     // Friendship blocked\n    }\n}\n