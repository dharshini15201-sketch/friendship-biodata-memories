package com.friendship.config;\n\nimport org.springframework.context.annotation.Bean;\nimport org.springframework.context.annotation.Configuration;\nimport org.springframework.web.cors.CorsConfiguration;\nimport org.springframework.web.cors.CorsConfigurationSource;\nimport org.springframework.web.cors.UrlBasedCorsConfigurationSource;\nimport org.springframework.web.servlet.config.annotation.WebMvcConfigurer;\n\nimport java.util.Arrays;\nimport java.util.Collections;\n\n/**\n * CORS Configuration for allowing cross-origin requests from frontend\n * \n * What is CORS?\n * CORS = Cross-Origin Resource Sharing\n * Problem: Frontend runs on http://localhost:3000 but Backend runs on http://localhost:8080\n * Solution: CORS allows them to communicate\n * \n * Without CORS, the browser blocks frontend->backend requests for security reasons\n */\n@Configuration\npublic class CorsConfig implements WebMvcConfigurer {\n\n    /**\n     * Configure CORS settings\n     * This tells the backend: \"Allow requests from these origins\"\n     */\n    @Bean\n    public CorsConfigurationSource corsConfigurationSource() {\n        CorsConfiguration configuration = new CorsConfiguration();\n\n        // Allow requests from these frontend URLs\n        configuration.setAllowedOrigins(Arrays.asList(\n            \"http://localhost:3000\",      // Development frontend\n            \"http://localhost:8080\",      // Alternative port\n            \"http://127.0.0.1:3000\"       // IP address version\n        ));\n\n        // Allow these HTTP methods (GET, POST, etc.)\n        configuration.setAllowedMethods(Arrays.asList(\n            \"GET\", \"POST\", \"PUT\", \"DELETE\", \"OPTIONS\", \"PATCH\"\n        ));\n\n        // Allow all headers (like Authorization, Content-Type)\n        configuration.setAllowedHeaders(Collections.singletonList(\"*\"));\n\n        // Allow cookies and authentication headers\n        configuration.setAllowCredentials(true);\n\n        // Cache preflight requests for 1 hour\n        configuration.setMaxAge(3600L);\n\n        // Apply CORS to all endpoints\n        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();\n        source.registerCorsConfiguration(\"/**\", configuration);\n        return source;\n    }\n}\n